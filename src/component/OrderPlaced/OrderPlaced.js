import React from 'react';
import image from '../../images/giphy.gif';

const OrderPlaced = () => {
    return (
        <div>
            <img src={image} alt="" />
        </div>
    );
};

export default OrderPlaced;